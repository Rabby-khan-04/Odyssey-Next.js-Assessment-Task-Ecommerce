export const metadata = {
  title: "NexCart - Sign Up",
  description: "Modern ecommerce platform for fast and easy shopping",
};

export default function SignupLayout({ children }) {
  return <main>{children}</main>;
}
